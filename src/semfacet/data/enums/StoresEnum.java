/**
 * This enum is used for different triple stores, so it is easier later on to add new ones and handle them. 
 */

package semfacet.data.enums;

public enum StoresEnum {
    JRDFOX, PAGODA, SESAME, HERMIT;
}
